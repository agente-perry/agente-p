# Deferred Specs

These specs are intentionally outside the hackathon MVP.

The current MVP is AgentePerry TDR Scanner. Do not implement deferred specs unless a maintainer moves them back to `specs/active/` and updates `README.md`, `AGENTS.md`, and `TEAM.md`.
