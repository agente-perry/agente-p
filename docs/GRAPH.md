# Graph — Deferred

Graph, ConflictMap, Graphiti and Neo4j are outside the MVP.

For the current sprint, use only the TDR tables in `packages/db/migrations/0002_tdr_core.sql`.

Historical graph notes are summarized in `docs/reference/legacy-platform.md`.
